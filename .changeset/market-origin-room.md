---
"screeps-client": patch
---

Market: remember the room you opened it from (carried in the `?room` URL query) and pre-fill it into the resource "Target room" box, so shipping-range distances are calculated against your current room right away. Still freely editable.
